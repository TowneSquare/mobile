import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const SpaceTab = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-center">SpaceTab</Text>
      </View>
    </SafeAreaView>
  );
};

export default SpaceTab;
